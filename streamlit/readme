We are building a streamlit app to help us manage our data. Because we do not need react, we can use the streamlit library to build the app.

We will use the streamlit library to build the app.

We will use the streamlit library to build the app.

We will use the streamlit library to build the app.